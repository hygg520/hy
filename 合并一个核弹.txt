local Rayfield = loadstring(game:HttpGet('https://raw.githubusercontent.com/hygg520/hy/refs/heads/main/Rayfield%20UI.txt'))() local Window = Rayfield:CreateWindow({ Name = "合并一个核弹", LoadingTitle = "hygg脚本", LoadingSubtitle = "正在启动...", ConfigurationSaving = { Enabled = false, FolderName = "hyggMerger", FileName = "hyggConfig" }, KeySystem = false, Theme = "Amethyst", ToggleUIKeybind = Enum.KeyCode.RightAlt }) local Players = game:GetService("Players") local LocalPlayer = Players.LocalPlayer local PlayerId = tonumber(LocalPlayer.UserId) local RunService = game:GetService("RunService") local Workspace = game:GetService("Workspace") local UserInputService = game:GetService("UserInputService") local ReplicatedStorage = game:GetService("ReplicatedStorage") local Main = Window:CreateTab("核弹核心", 4483362458) local mergeInterval = 0.1 local pickupInterval = 0.5 local currentHeldTier = nil local teleportFailCount = 0 local MAX_FAIL_COUNT = 3 local isScanning = false local R_HoldStarted = ReplicatedStorage.NukeRemotes:FindFirstChild("HoldStarted") local R_HoldEnded = ReplicatedStorage.NukeRemotes:FindFirstChild("HoldEnded") if R_HoldStarted then R_HoldStarted.OnClientEvent:Connect(function(tier) if currentHeldTier ~= nil and currentHeldTier == tier then teleportFailCount = teleportFailCount + 1 elseif currentHeldTier ~= nil and currentHeldTier ~= tier then teleportFailCount = 0 end currentHeldTier = tier end) end if R_HoldEnded then R_HoldEnded.OnClientEvent:Connect(function() currentHeldTier = nil teleportFailCount = 0 isScanning = false end) end local function GetPlayerBase() local BasesFolder = Workspace:FindFirstChild("Bases") if BasesFolder then for _, folder in ipairs(BasesFolder:GetChildren()) do local attributeValue = folder:GetAttribute("OwnerUserId") if attributeValue and tonumber(attributeValue) == PlayerId then return folder end end end return nil end local function TeleportTo(object) if not object or not LocalPlayer.Character then return end local root = LocalPlayer.Character:FindFirstChild("HumanoidRootPart") local position = nil if object:IsA("Model") then position = object:GetPivot().Position elseif object:IsA("BasePart") then position = object.Position end if root and position then root.CFrame = CFrame.new(position + Vector3.new(0, 0.5, 0)) end end local function GetNearestNukesInSquare(nukesFolder) local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") if not root then return {} end local playerPos = root.Position local groupedNukes = {} local RANGE = 500 for _, nuke in ipairs(nukesFolder:GetChildren()) do if nuke.Name == "Nuke" and nuke:FindFirstChild("OverheadNuke") and nuke.OverheadNuke:FindFirstChild("TextLabel") then local nukePos = nuke:GetPivot().Position local diffX = math.abs(nukePos.X - playerPos.X) local diffZ = math.abs(nukePos.Z - playerPos.Z) if diffX <= RANGE and diffZ <= RANGE then local nukeType = nuke.OverheadNuke.TextLabel.Text if nukeType and nukeType ~= "" then if not groupedNukes[nukeType] then groupedNukes[nukeType] = {} end table.insert(groupedNukes[nukeType], { Model = nuke, Distance = (nukePos - playerPos).Magnitude }) end end end end for _, group in pairs(groupedNukes) do table.sort(group, function(a, b) return a.Distance < b.Distance end) end return groupedNukes end local function ScanAllNukesByTier(targetTier) local myBase = GetPlayerBase() if not myBase then return nil end local nukesFolder = myBase:FindFirstChild("Nukes") if not nukesFolder then return nil end local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") if not root then return nil end local playerPos = root.Position local foundNukes = {} for _, nuke in ipairs(nukesFolder:GetChildren()) do if nuke.Name == "Nuke" and nuke:FindFirstChild("OverheadNuke") and nuke.OverheadNuke:FindFirstChild("TextLabel") then local tierText = nuke.OverheadNuke.TextLabel.Text if tierText and tierText ~= "" then local tier = tonumber(tierText) if tier == targetTier then table.insert(foundNukes, { Model = nuke, Distance = (nuke:GetPivot().Position - playerPos).Magnitude }) end end end end table.sort(foundNukes, function(a, b) return a.Distance < b.Distance end) return foundNukes end local dropRecords = {} local DROP_RECORD_EXPIRE = 4 local function recordDropPosition(pos) table.insert(dropRecords, { pos = pos, time = tick() }) for i = #dropRecords, 1, -1 do if tick() - dropRecords[i].time > DROP_RECORD_EXPIRE then table.remove(dropRecords, i) end end end local function isNearDropZone(pos, radius) radius = radius or 10 for _, record in ipairs(dropRecords) do local dx = pos.X - record.pos.X local dz = pos.Z - record.pos.Z if math.sqrt(dx*dx + dz*dz) < radius then return true end end return false end local function DropHeldNuke() local R_Drop = ReplicatedStorage.NukeRemotes.Drop local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") local dropPos = hrp and hrp.Position or Vector3.new(0, 0, 0) pcall(function() R_Drop:FireServer() end) recordDropPosition(dropPos) serverHeldTier = nil currentHeldTier = nil teleportFailCount = 0 task.wait(0.5) local base = GetPlayerBase() if base and hrp then local center = base:GetPivot().Position hrp.CFrame = CFrame.new(center + Vector3.new(0, 0.5, 0)) end task.wait(0.3) end local function ShouldSkipPickup(nukePos, radius) radius = radius or 10 return isNearDropZone(nukePos, radius) end local function SmartDrop() DropHeldNuke() end local function nearRecentDrop(worldPos, ignoreTier) return isNearDropZone(worldPos, 10) end local function separateNuke(nukeToMove, referencePos) if not nukeToMove or not nukeToMove.Parent then return end local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") if not hrp then return end local nukePos = nukeToMove:GetPivot().Position local dir = (nukePos - referencePos).Unit local targetPos = referencePos + dir * 18 hrp.CFrame = CFrame.new(targetPos + Vector3.new(0, 0.5, 0)) task.wait(0.2) end local function DoMergeCycle() local R_PickUp = ReplicatedStorage.NukeRemotes.PickUp local R_Merge = ReplicatedStorage.NukeRemotes.MergeRequest if teleportFailCount >= MAX_FAIL_COUNT and currentHeldTier ~= nil and not isScanning then isScanning = true local heldTier = currentHeldTier local scanResults = ScanAllNukesByTier(heldTier) if scanResults and #scanResults >= 1 then local target = scanResults[1].Model TeleportTo(target) task.wait(0.3) pcall(function() R_Merge:FireServer(target) end) task.wait(0.3) if currentHeldTier ~= heldTier then teleportFailCount = 0 end else DropHeldNuke() teleportFailCount = 0 end isScanning = false return end if serverHeldTier ~= nil then local heldTier = serverHeldTier local myBase = GetPlayerBase() if myBase and myBase:FindFirstChild("Nukes") then local groupedNukes = GetNearestNukesInSquare(myBase.Nukes) local targets = groupedNukes[tostring(heldTier)] if targets and #targets >= 1 then TeleportTo(targets[1].Model) task.wait(0.3) pcall(function() R_Merge:FireServer(targets[1].Model) end) task.wait(0.3) else DropHeldNuke() end else DropHeldNuke() end task.wait(mergeInterval) return end local myBase = GetPlayerBase() if myBase and myBase:FindFirstChild("Nukes") then local groupedNukes = GetNearestNukesInSquare(myBase.Nukes) for tier, matches in pairs(groupedNukes) do if #matches >= 2 then local firstNuke = matches[1].Model local secondNuke = matches[2].Model if ShouldSkipPickup(firstNuke:GetPivot().Position, 10) then continue end local nukesFolder = myBase.Nukes for _, other in ipairs(nukesFolder:GetChildren()) do if other ~= firstNuke and other.Name == "Nuke" and other:FindFirstChild("OverheadNuke") and other.OverheadNuke:FindFirstChild("TextLabel") then local otherTier = tonumber(other.OverheadNuke.TextLabel.Text) local thisTier = tonumber(tier) if otherTier and otherTier ~= thisTier then local dist = (firstNuke:GetPivot().Position - other:GetPivot().Position).Magnitude if dist <= 4 then separateNuke(other, firstNuke:GetPivot().Position) task.wait(0.1) break end end end end TeleportTo(firstNuke) task.wait(0.3) pcall(function() R_PickUp:FireServer(firstNuke) end) task.wait(0.2) if serverHeldTier == nil and currentHeldTier == nil then task.wait(0.3) continue end TeleportTo(secondNuke) task.wait(0.3) pcall(function() R_Merge:FireServer(secondNuke) end) task.wait(0.2) break end end end task.wait(mergeInterval) end Main:CreateSection("自动核弹合并") Main:CreateToggle({ Name = "自动合并 (1000x1000 范围 + 防错)", CurrentValue = false, Flag = "AutoMergeToggle", Callback = function(Value) _G.AutoMerge = Value while _G.AutoMerge do local ok, err = pcall(DoMergeCycle) if not ok then if serverHeldTier or currentHeldTier then DropHeldNuke() end serverHeldTier = nil currentHeldTier = nil end end end }) Main:CreateToggle({ Name = "自动拾取所有核弹", CurrentValue = false, Flag = "AutoPickUpToggle", Callback = function(Value) _G.AutoPickUp = Value while _G.AutoPickUp do local myBase = GetPlayerBase() if myBase and myBase:FindFirstChild("Nukes") then local nukeCounts = {} for _, nuke in ipairs(myBase.Nukes:GetChildren()) do if nuke.Name == "Nuke" and nuke:FindFirstChild("OverheadNuke") and nuke.OverheadNuke:FindFirstChild("TextLabel") then local nukeType = nuke.OverheadNuke.TextLabel.Text if nukeType and nukeType ~= "" then if not nukeCounts[nukeType] then nukeCounts[nukeType] = {} end table.insert(nukeCounts[nukeType], nuke) end end end for _, nuke in ipairs(myBase.Nukes:GetChildren()) do if not _G.AutoPickUp then break end if nuke.Name == "Nuke" and nuke:FindFirstChild("OverheadNuke") and nuke.OverheadNuke:FindFirstChild("TextLabel") then local nukeType = nuke.OverheadNuke.TextLabel.Text local matchCount = nukeCounts[nukeType] and #nukeCounts[nukeType] or 0 local PickUpEvent = ReplicatedStorage.NukeRemotes.PickUp local rootPart = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") local originalCFrame = rootPart and rootPart.CFrame TeleportTo(nuke) task.wait() PickUpEvent:FireServer(nuke) task.wait() if matchCount < 2 then local DropEvent = ReplicatedStorage.NukeRemotes.Drop if rootPart then DropEvent:FireServer(rootPart.CFrame) else DropEvent:FireServer(CFrame.new(290.03, 17.20, 249.74)) end task.wait() end if rootPart and originalCFrame then rootPart.CFrame = originalCFrame end end end end task.wait() end end }) Main:CreateSection("基地防御与升级") local upgradeThreads = {} local function startAutoUpgrade(key) if upgradeThreads[key] then task.cancel(upgradeThreads[key]) upgradeThreads[key] = nil end upgradeThreads[key] = task.spawn(function() while true do local remotes = ReplicatedStorage:FindFirstChild("NukeRemotes") if remotes and remotes:FindFirstChild("PurchaseUpgrade") then local Event = remotes.PurchaseUpgrade pcall(function() Event:FireServer(key) end) end task.wait(0.5) end end) end local function stopAutoUpgrade(key) if upgradeThreads[key] then task.cancel(upgradeThreads[key]) upgradeThreads[key] = nil end end Main:CreateToggle({ Name = "生成等级", CurrentValue = false, Flag = "AutoUpgrade_TIER", Callback = function(Value) if Value then startAutoUpgrade("TIER") else stopAutoUpgrade("TIER") end end }) Main:CreateToggle({ Name = "最大生成数", CurrentValue = false, Flag = "AutoUpgrade_MAX", Callback = function(Value) if Value then startAutoUpgrade("MAX") else stopAutoUpgrade("MAX") end end }) Main:CreateToggle({ Name = "锁定冷却", CurrentValue = false, Flag = "AutoUpgrade_LOCKBASE", Callback = function(Value) if Value then startAutoUpgrade("LOCKBASE") else stopAutoUpgrade("LOCKBASE") end end })
