local Rayfield = loadstring(game:HttpGet('https://raw.githubusercontent.com/hygg520/hy/refs/heads/main/Rayfield%20UI.txt'))() local Window = Rayfield:CreateWindow({ Name = "英雄随机生成", LoadingTitle = "hygg", LoadingSubtitle = "正在加载...", ConfigurationSaving = {Enabled = false}, KeySystem = false, }) local env = (getfenv and getfenv()) or _G local genv = (env.getgenv and env.getgenv()) or env local getconnections = genv.getconnections or (rawget and rawget(genv, "getconnections")) local firesignal = genv.firesignal or (rawget and rawget(genv, "firesignal")) local fireproximityprompt = genv.fireproximityprompt or (rawget and rawget(genv, "fireproximityprompt")) local fireclickdetector = genv.fireclickdetector or (rawget and rawget(genv, "fireclickdetector")) local Players = game:GetService("Players") local Workspace = game:GetService("Workspace") local RunService = game:GetService("RunService") local UserInputService = game:GetService("UserInputService") local ReplicatedStorage = game:GetService("ReplicatedStorage") local VirtualUser = game:GetService("VirtualUser") local LocalPlayer = Players.LocalPlayer while not LocalPlayer do task.wait(0.1) LocalPlayer = Players.LocalPlayer end local Remotes = ReplicatedStorage:WaitForChild("Remotes", 5) or ReplicatedStorage local Config = { AutoUpgrade = false, UpgradeDelay = 0.5, AutoPrestige = false, SpeedRoll = false, SpeedRollDelay = 0.08, AutoCollectLoot = false, MaxLootRadius = 140, } local FarmZoneAnchor = nil local isZoneTeleporting = false local lootPauseUntil = 0 local Character = LocalPlayer.Character local Humanoid = Character and Character:FindFirstChildOfClass("Humanoid") local RootPart = Character and (Character:FindFirstChild("HumanoidRootPart") or Character.PrimaryPart) local function updateChar(char) Character = char if char then Humanoid = char:WaitForChild("Humanoid", 3) or char:FindFirstChildOfClass("Humanoid") RootPart = char:WaitForChild("HumanoidRootPart", 3) or char.PrimaryPart end end LocalPlayer.CharacterAdded:Connect(updateChar) if Character then updateChar(Character) end LocalPlayer.Idled:Connect(function() pcall(function() local bb = game:GetService("VirtualUser") if bb then bb:CaptureController() bb:ClickButton2(Vector2.new(0, 0)) end end) end) local function silentClick(btn, btnName) if not btn then return false end btnName = btnName or btn.Name or "Object" pcall(function() if firesignal then for _, evt in ipairs({"MouseButton1Down", "MouseButton1Click", "MouseButton1Up", "Activated", "TouchTap"}) do pcall(function() firesignal(btn, evt) end) end end if getconnections then for _, evt in ipairs({"MouseButton1Click", "Activated", "MouseButton1Down", "MouseButton1Up", "TouchTap", "InputBegan"}) do pcall(function() local conns = getconnections(btn[evt]) or {} for _, c in pairs(conns) do if c.Fire then c:Fire() end if c.fire then c:fire() end end end) end end if btn:IsA("GuiButton") and btn.Activated then pcall(function() btn.Activated:Fire() end) end end) return true end local function parseNumber(str) if not str then return 0 end if type(str) == "number" then return str end local clean = string.gsub(tostring(str), "[,%s%$]+", "") clean = string.gsub(clean, "[^0-9%.kKmMbBtTqQqQsSpP]", "") local num, suffix = string.match(clean, "^([%d%.]+)([a-zA-Z]?)$") if not num then num = string.match(clean, "%d+%.?%d*") if not num then return 0 end return tonumber(num) or 0 end local val = tonumber(num) or 0 suffix = string.lower(suffix or "") if suffix == "k" then return val * 1e3 elseif suffix == "m" then return val * 1e6 elseif suffix == "b" then return val * 1e9 elseif suffix == "t" then return val * 1e12 elseif suffix == "q" then return val * 1e15 elseif suffix == "qi" then return val * 1e18 elseif suffix == "sx" then return val * 1e21 elseif suffix == "sp" then return val * 1e24 end return val end local function formatNumberShort(num) if not num or num == 0 then return "0" end if num >= 1e15 then return string.format("%.2fQ", num / 1e15) elseif num >= 1e12 then return string.format("%.2fT", num / 1e12) elseif num >= 1e9 then return string.format("%.2fB", num / 1e9) elseif num >= 1e6 then return string.format("%.2fM", num / 1e6) elseif num >= 1e3 then return string.format("%.2fK", num / 1e3) end return tostring(math.floor(num)) end local function getExactGold() local pGui = LocalPlayer:FindFirstChild("PlayerGui") if pGui then local main = pGui:FindFirstChild("Main") if main then local left = main:FindFirstChild("Left") local cur = left and left:FindFirstChild("Currencies") local goldNode = cur and cur:FindFirstChild("Gold") local qty = goldNode and goldNode:FindFirstChild("Quantity") if qty and qty:IsA("TextLabel") and qty.Text ~= "" then return parseNumber(qty.Text) end end end local leaderstats = LocalPlayer:FindFirstChild("leaderstats") if leaderstats then local g = leaderstats:FindFirstChild("Gold") if g then return parseNumber(g.Value) end end return 0 end local function getLevelInfo() local curLvl = 1 local reqLvl = 30 local leaderstats = LocalPlayer:FindFirstChild("leaderstats") if leaderstats then local lvl = leaderstats:FindFirstChild("Level") if lvl then curLvl = tonumber(lvl.Value) or 1 end end local pGui = LocalPlayer:FindFirstChild("PlayerGui") if pGui then local prestigeFrame = pGui:FindFirstChild("Main") and pGui.Main:FindFirstChild("Center") and pGui.Main.Center:FindFirstChild("Prestige") if prestigeFrame then local lvlBar = prestigeFrame:FindFirstChild("LevelBar") for _, d in ipairs(lvlBar and lvlBar:GetDescendants() or prestigeFrame:GetDescendants()) do if d:IsA("TextLabel") and string.find(d.Text, "Level") then local c, r = string.match(d.Text, "Level%s*(%d+)%s*/%s*(%d+)") if c and r then return tonumber(c) or curLvl, tonumber(r) or reqLvl end end end end end return curLvl, reqLvl end local VerifiedZoneFallbacks = { Starter = Vector3.new(0, 38, 0), Meadows = Vector3.new(12, 38, 65), Forest = Vector3.new(-80, 38, 200), DarkForest = Vector3.new(-80, 38, 420), Beach = Vector3.new(45, 38, 240), Sea = Vector3.new(290, 38, -480), IceCave = Vector3.new(-800, 55, 1800), Arctic = Vector3.new(520, 48, -480), Jungle = Vector3.new(300, 45, 750), Badlands = Vector3.new(-350, 45, 900), Volcano = Vector3.new(400, 50, 1200), Shadowgrove = Vector3.new(500, 50, 1100), SerpentSands = Vector3.new(-600, 50, 1300), Void = Vector3.new(800, 60, 1600), Next = Vector3.new(1000, 65, 2000) } local function getElevatedZoneCFrame(zoneKey) local zonesFolder = Workspace:FindFirstChild("Zones") if zonesFolder then for _, z in ipairs(zonesFolder:GetChildren()) do local zName = string.lower(z.Name) local targetKey = string.lower(zoneKey) if zName == targetKey or string.find(zName, targetKey) or string.find(targetKey, zName) then local spawnPart = z:FindFirstChild("Spawn", true) or z:FindFirstChild("SpawnLocation", true) or z:FindFirstChild("Teleport", true) or z:FindFirstChild("Part", true) or z.PrimaryPart if spawnPart and spawnPart:IsA("BasePart") then return spawnPart.CFrame + Vector3.new(0, 8, 0) end end end end local vec = VerifiedZoneFallbacks[zoneKey] or Vector3.new(0, 40, 0) return CFrame.new(vec + Vector3.new(0, 6, 0)) end local originalTeleportPos = nil local function ensureTeleportUIAliveOffscreen() local pGui = LocalPlayer:FindFirstChild("PlayerGui") local main = pGui and pGui:FindFirstChild("Main") local center = main and main:FindFirstChild("Center") local teleport = center and center:FindFirstChild("Teleport") or (main and main:FindFirstChild("Teleport", true)) if teleport then if not originalTeleportPos and teleport.Position.X.Scale < 5 then originalTeleportPos = teleport.Position end teleport.Visible = true teleport.Position = UDim2.new(999, 0, 999, 0) end end local function teleportToZoneSafely(zoneKey, displayName) displayName = displayName or zoneKey isZoneTeleporting = true lootPauseUntil = tick() + 2.0 pcall(ensureTeleportUIAliveOffscreen) local pGui = LocalPlayer:FindFirstChild("PlayerGui") local main = pGui and pGui:FindFirstChild("Main") local tpFrame = main and (main:FindFirstChild("Center") and main.Center:FindFirstChild("Teleport")) or (main and main:FindFirstChild("Teleport", true)) if tpFrame then for _, desc in ipairs(tpFrame:GetDescendants()) do if desc:IsA("GuiObject") and (string.lower(desc.Name) == string.lower(zoneKey) or string.find(string.lower(desc.Name), string.lower(zoneKey)) or string.find(string.lower(desc.Name), string.lower(displayName))) then for _, b in ipairs(desc:GetDescendants()) do if b:IsA("GuiButton") then silentClick(b, "传送按钮_" .. zoneKey) end end if desc:IsA("GuiButton") then silentClick(desc, "传送框架_" .. zoneKey) end end end end local remNames = {"RequestZoneTeleport", "TeleportZone", "Teleport", "TeleportToZone", "ZoneTeleport"} for _, rName in ipairs(remNames) do local r = Remotes:FindFirstChild(rName) if r then pcall(function() if r:IsA("RemoteFunction") then task.spawn(function() r:InvokeServer(zoneKey) end) task.spawn(function() r:InvokeServer(displayName) end) else r:FireServer(zoneKey) r:FireServer(displayName) end end) end end local targetCFrame = getElevatedZoneCFrame(zoneKey) if RootPart then RootPart.CFrame = targetCFrame FarmZoneAnchor = targetCFrame end task.spawn(function() task.wait(1.8) if RootPart then FarmZoneAnchor = RootPart.CFrame end isZoneTeleporting = false end) end local function executeSmartLootCollection() if not RootPart or not Config.AutoCollectLoot then return end if isZoneTeleporting or tick() < lootPauseUntil then return end if not FarmZoneAnchor then FarmZoneAnchor = RootPart.CFrame end local anchorPos = FarmZoneAnchor.Position local candidateDrops = {} local function evaluateDrop(obj) if not obj or not obj.Parent then return end if Character and obj:IsDescendantOf(Character) then return end local part = nil if obj:IsA("BasePart") then part = obj elseif obj:IsA("Model") and obj.PrimaryPart then part = obj.PrimaryPart elseif obj:IsA("Model") then part = obj:FindFirstChildWhichIsA("BasePart") end if not part then return end local pName = string.lower(obj.Name) local parentName = string.lower(obj.Parent.Name) local isGold = string.find(pName, "gold") or string.find(parentName, "gold") local isXP = string.find(pName, "xp") or string.find(parentName, "xp") local isBeans = string.find(pName, "bean") or string.find(parentName, "bean") or string.find(pName, "cube") local isGeneralLoot = string.find(parentName, "loot") or string.find(pName, "loot") if isGold or isXP or isBeans or isGeneralLoot then local distFromAnchor = (part.Position - anchorPos).Magnitude if distFromAnchor <= Config.MaxLootRadius then local distFromPlayer = (RootPart.Position - part.Position).Magnitude table.insert(candidateDrops, { Part = part, Model = obj, Position = part.Position, DistAnchor = distFromAnchor, DistPlayer = distFromPlayer, IsBeans = isBeans, IsGold = isGold }) end end end local lootFolder = Workspace:FindFirstChild("Loot") if lootFolder then for _, drop in ipairs(lootFolder:GetChildren()) do evaluateDrop(drop) end else for _, desc in ipairs(Workspace:GetChildren()) do if desc.Name == "LootRender_Gold" or desc.Name == "LootRender_XP" or desc.Name == "LootRender_Beans" then evaluateDrop(desc) end end end if #candidateDrops == 0 then return end table.sort(candidateDrops, function(a, b) if a.IsBeans ~= b.IsBeans then return a.IsBeans end return a.DistPlayer < b.DistPlayer end) for i = 1, math.min(#candidateDrops, 5) do local target = candidateDrops[i] if target.Part and target.Part.Parent then RootPart.CFrame = CFrame.new(target.Position + Vector3.new(0, 1.0, 0)) pcall(function() if firesignal and target.Part.Touched then firesignal(target.Part.Touched, RootPart) end end) task.wait(0.06) end end end local branchCycleIndex = 1 local lastBranchSwitch = 0 local function executeSmartUpgrades() local myGold = getExactGold() local pGui = LocalPlayer:FindFirstChild("PlayerGui") if not pGui then return end local main = pGui:FindFirstChild("Main") if not main then return end local upgradesFrame = main:FindFirstChild("Upgrades") local canvas = upgradesFrame and upgradesFrame:FindFirstChild("Canvas") local content = canvas and canvas:FindFirstChild("Content") local backBtn = upgradesFrame and (upgradesFrame:FindFirstChild("Back") or upgradesFrame:FindFirstChild("BackBtn", true)) if not content then return end local availableTiles = {} local branchTiles = {} for _, tile in ipairs(content:GetChildren()) do if string.find(tile.Name, "Tile_") then local rawName = string.gsub(tile.Name, "Tile_", "") local openLbl = tile:FindFirstChild("OpenText") or tile:FindFirstChild("BranchProgress") if openLbl or string.find(rawName, "Combat") or string.find(rawName, "Loot") or string.find(rawName, "Player") then local clickBtn = tile:IsA("GuiButton") and tile or tile:FindFirstChildWhichIsA("GuiButton", true) or tile table.insert(branchTiles, { Name = rawName, Button = clickBtn, Tile = tile }) end local costLabel = tile:FindFirstChild("CostText") if costLabel and costLabel:IsA("TextLabel") and costLabel.Text ~= "" and costLabel.Text ~= "?" and costLabel.Text ~= "MAX" then local costVal = parseNumber(costLabel.Text) if costVal > 0 and costVal <= myGold then local clickBtn = tile:IsA("GuiButton") and tile or tile:FindFirstChildWhichIsA("GuiButton", true) or tile table.insert(availableTiles, { Id = rawName, Name = rawName, Cost = costVal, Button = clickBtn, Tile = tile }) end end end end if #availableTiles > 0 then table.sort(availableTiles, function(a, b) return a.Cost < b.Cost end) local target = availableTiles[1] if Remotes:FindFirstChild("PurchaseUpgrade") then Remotes.PurchaseUpgrade:FireServer(target.Id) Remotes.PurchaseUpgrade:FireServer(target.Name) end if target.Button then silentClick(target.Button, target.Name) end return end local now = tick() if myGold >= 5000 and (now - lastBranchSwitch) >= 3.0 then lastBranchSwitch = now if backBtn and backBtn.Visible then silentClick(backBtn, "升级返回") return end if #branchTiles > 0 then local targetBranch = branchTiles[branchCycleIndex] if targetBranch and targetBranch.Button then silentClick(targetBranch.Button, targetBranch.Name) branchCycleIndex = (branchCycleIndex % #branchTiles) + 1 end end end end local lastPrestigeAttempt = 0 local function executeSmartPrestige() local curLvl, reqLvl = getLevelInfo() local now = tick() if curLvl >= reqLvl and reqLvl > 0 and (now - lastPrestigeAttempt) >= 10 then lastPrestigeAttempt = now if Remotes:FindFirstChild("PrestigeRequested") then Remotes.PrestigeRequested:FireServer() end local pGui = LocalPlayer:FindFirstChild("PlayerGui") local prestigeBtn = pGui and pGui:FindFirstChild("Main") and pGui.Main:FindFirstChild("Center") and pGui.Main.Center:FindFirstChild("Prestige") and pGui.Main.Center.Prestige:FindFirstChild("Prestige") if prestigeBtn then silentClick(prestigeBtn, "转生按钮") end end pcall(function() local prestigeUpgradesList = { "Prestige Roll Speed I", "Prestige Gold I", "Prestige Gold II", "Prestige XP I", "Hero Atk Speed I", "Hero Atk Speed II", "Prestige Luck I", "Prestige Luck II", "Prestige Luck III", "Prestige Damage I", "Prestige Damage II", "Prestige Damage III" } for _, uName in ipairs(prestigeUpgradesList) do if Remotes:FindFirstChild("PurchasePrestigeUpgrade") then Remotes.PurchasePrestigeUpgrade:FireServer(uName) Remotes.PurchasePrestigeUpgrade:FireServer(uName:gsub("%s+", "")) end end end) end local function executeSpeedRoll() if Remotes:FindFirstChild("Roll") then if Remotes.Roll:IsA("RemoteFunction") then task.spawn(function() pcall(function() Remotes.Roll:InvokeServer() end) end) elseif Remotes.Roll:IsA("RemoteEvent") then Remotes.Roll:FireServer() end end if Remotes:FindFirstChild("SetAutoRoll") then Remotes.SetAutoRoll:FireServer(true) end end task.spawn(function() while true do if Config.AutoCollectLoot then pcall(executeSmartLootCollection) end task.wait(0.12) end end) task.spawn(function() while true do if Config.AutoUpgrade then pcall(executeSmartUpgrades) end task.wait(Config.UpgradeDelay) end end) task.spawn(function() while true do if Config.AutoPrestige then pcall(executeSmartPrestige) end task.wait(3.0) end end) task.spawn(function() while true do if Config.SpeedRoll then pcall(executeSpeedRoll) task.wait(Config.SpeedRollDelay) else task.wait(0.5) end end end) local MainTab = Window:CreateTab("主要功能", nil) MainTab:CreateSection("自动拾取") MainTab:CreateToggle({ Name = "自动拾取", CurrentValue = false, Flag = "AutoCollectLoot", Callback = function(v) Config.AutoCollectLoot = v if v and RootPart then FarmZoneAnchor = RootPart.CFrame end end, }) MainTab:CreateSlider({ Name = "自动拾取半径", Range = {50, 300}, Increment = 5, Suffix = " 单位", CurrentValue = 140, Flag = "MaxLootRadius", Callback = function(v) Config.MaxLootRadius = v end, }) MainTab:CreateSection("自动升级") MainTab:CreateToggle({ Name = "自动升级", CurrentValue = false, Flag = "AutoUpgrade", Callback = function(v) Config.AutoUpgrade = v end, }) MainTab:CreateSlider({ Name = "升级速度延迟", Range = {0.2, 2.0}, Increment = 0.1, Suffix = "秒", CurrentValue = 0.5, Flag = "UpgradeDelay", Callback = function(v) Config.UpgradeDelay = v end, }) MainTab:CreateToggle({ Name = "自动重生并花费金钱", CurrentValue = false, Flag = "AutoPrestige", Callback = function(v) Config.AutoPrestige = v end, }) MainTab:CreateButton({ Name = "升级一次最便宜", Callback = function() executeSmartUpgrades() end, }) MainTab:CreateSection("快速抽奖") MainTab:CreateToggle({ Name = "快速抽奖（跳过动画）", CurrentValue = false, Flag = "SpeedRoll", Callback = function(v) Config.SpeedRoll = v end, }) MainTab:CreateSlider({ Name = "抽奖延迟", Range = {0.01, 0.5}, Increment = 0.01, Suffix = "秒", CurrentValue = 0.08, Flag = "SpeedRollDelay", Callback = function(v) Config.SpeedRollDelay = v end, }) local TeleportTab = Window:CreateTab("传送", nil) TeleportTab:CreateSection("传送") local VerifiedZonesList = { { Display = "草原", Key = "Meadows" }, { Display = "黑暗森林 [融合]", Key = "Forest" }, { Display = "海滩 [符文]", Key = "Beach" }, { Display = "大海", Key = "Sea" }, { Display = "冰洞", Key = "IceCave" }, { Display = "北极", Key = "Arctic" }, { Display = "丛林", Key = "Jungle" }, { Display = "荒地 [塔]", Key = "Badlands" }, { Display = "火山", Key = "Volcano" }, { Display = "暗影林", Key = "Shadowgrove" }, { Display = "蛇沙", Key = "SerpentSands" }, { Display = "虚空", Key = "Void" }, } for _, zone in ipairs(VerifiedZonesList) do TeleportTab:CreateButton({ Name = zone.Display, Callback = function() teleportToZoneSafely(zone.Key, zone.Display) end, }) end Rayfield:Notify({ Title = "加载完毕", Content = "英雄随机生成脚本已加载", Duration = 4, })