local Rayfield = loadstring(game:HttpGet('https://raw.githubusercontent.com/hygg520/hy/refs/heads/main/Rayfield%20UI.txt'))() local Window = Rayfield:CreateWindow({ Name = "浪费时间", LoadingTitle = "hygg", LoadingSubtitle = "正在加载...", ConfigurationSaving = {Enabled = false}, KeySystem = false, }) local Players = game:GetService("Players") local RunService = game:GetService("RunService") local UserInputService = game:GetService("UserInputService") local VirtualUser = game:GetService("VirtualUser") local LocalPlayer = Players.LocalPlayer local function getRoot() local c = LocalPlayer.Character return c and c:FindFirstChild("HumanoidRootPart") end local TeleportTab = Window:CreateTab("传送") TeleportTab:CreateSection("传送区域") local teleports = { {name = "出生点", pos = Vector3.new(-2, 3, -8)}, {name = "重生点", pos = Vector3.new(17.022, 3.476, 1.211)}, {name = "普通跑酷", pos = Vector3.new(-97.33, 4.59, -145.71)}, {name = "困难跑酷", pos = Vector3.new(-71.55, 3.88, -145.65)}, {name = "图书馆 - 1M时间", pos = Vector3.new(20.00, 51.12, 30.08)}, {name = "未来 - 50Qa时间", pos = Vector3.new(2042.76, 1375.78, -2879.17)}, {name = "虚空 - 1Td时间", pos = Vector3.new(1503.81, 1348.27, -2112.80)}, {name = "天顶 - 1Tvg时间", pos = Vector3.new(1673.55, 381.37, -2855.26)}, {name = "地狱 - 1Qitg时间", pos = Vector3.new(1197.04, 86.54, -2847.24)}, {name = "网络 - 1Spqag时间", pos = Vector3.new(1062.17, 1045.20, -2718.12)}, } for _, tp in ipairs(teleports) do TeleportTab:CreateButton({ Name = tp.name, Callback = function() local root = getRoot() if root then root.CFrame = CFrame.new(tp.pos) end end, }) end local MiscTab = Window:CreateTab("杂项") local chestKeywords = {"crate"} local chestExclude = {"hitbox", "spawnbox", "textbox", "soundbox", "skybox", "toolbox", "box"} local function isChestObj(obj) if not (obj:IsA("Model") or obj:IsA("BasePart")) then return false end local name = string.lower(obj.Name) for _, ex in ipairs(chestExclude) do if string.find(name, ex, 1, true) then return false end end for _, kw in ipairs(chestKeywords) do if string.find(name, kw, 1, true) then return true end end return false end MiscTab:CreateSection("宝箱") local autoPickEnabled = false local autoPickTask = nil local function findNearestChest() local myRoot = getRoot() if not myRoot then return nil end local myPos = myRoot.Position local nearest = nil local nearestDist = math.huge local queue = {} for _, obj in ipairs(workspace:GetChildren()) do table.insert(queue, {obj = obj, depth = 0}) end local idx = 1 while idx <= #queue do local item = queue[idx] idx = idx + 1 local obj = item.obj local depth = item.depth if obj and obj.Parent then if isChestObj(obj) then local pos if obj:IsA("BasePart") then pos = obj.Position elseif obj:IsA("Model") then local ok, cf = pcall(function() return obj:GetBoundingBox() end) if ok then pos = cf.Position end end if pos then local d = (pos - myPos).Magnitude if d < nearestDist then nearestDist = d nearest = pos end end end if depth < 3 then for _, child in ipairs(obj:GetChildren()) do table.insert(queue, {obj = child, depth = depth + 1}) end end end end return nearest end MiscTab:CreateToggle({ Name = "自动拾取宝箱", CurrentValue = false, Callback = function(val) autoPickEnabled = val if val then if autoPickTask then task.cancel(autoPickTask) end autoPickTask = task.spawn(function() while autoPickEnabled do local myRoot = getRoot() if myRoot then local originCF = myRoot.CFrame local chestPos = findNearestChest() if chestPos then myRoot.CFrame = CFrame.new(chestPos + Vector3.new(0, 3, 0)) task.wait(1.5) if myRoot and myRoot.Parent then myRoot.CFrame = originCF end task.wait(0.15) end end task.wait(0.5) end end) else if autoPickTask then task.cancel(autoPickTask) autoPickTask = nil end end end, }) MiscTab:CreateButton({ Name = "拾取宝箱", Callback = function() local myRoot = getRoot() if not myRoot then Rayfield:Notify({Title = "错误", Content = "未找到角色", Duration = 2}) return end local originCF = myRoot.CFrame local chestPos = findNearestChest() if chestPos then myRoot.CFrame = CFrame.new(chestPos + Vector3.new(0, 3, 0)) task.wait(1.5) if myRoot and myRoot.Parent then myRoot.CFrame = originCF end Rayfield:Notify({Title = "拾取宝箱", Content = "已拾取一个宝箱", Duration = 2}) else Rayfield:Notify({Title = "拾取宝箱", Content = "附近没有找到宝箱", Duration = 2}) end end, }) MiscTab:CreateSection("重生") local autoRespawnConn = nil MiscTab:CreateToggle({ Name = "自动重生", CurrentValue = false, Callback = function(val) if val then if autoRespawnConn then autoRespawnConn:Disconnect() end autoRespawnConn = RunService.Heartbeat:Connect(function() local char = LocalPlayer.Character if char and char:FindFirstChild("HumanoidRootPart") then char.HumanoidRootPart.CFrame = CFrame.new(17, 1, 2) end end) else if autoRespawnConn then autoRespawnConn:Disconnect() autoRespawnConn = nil end end end, }) MiscTab:CreateSection("移动") local antiPullbackConn = nil local antiPullbackSpeed = 100 MiscTab:CreateToggle({ Name = "加速", CurrentValue = false, Callback = function(val) if val then if antiPullbackConn then antiPullbackConn:Disconnect() end antiPullbackConn = RunService.Heartbeat:Connect(function() local char = LocalPlayer.Character if not char then return end local hum = char:FindFirstChildOfClass("Humanoid") if not hum then return end if hum.MoveDirection.Magnitude > 0 then char:TranslateBy(hum.MoveDirection * antiPullbackSpeed / 20) end end) else if antiPullbackConn then antiPullbackConn:Disconnect() antiPullbackConn = nil end end end, }) MiscTab:CreateSection("视觉") local CHEST_TAG = "NH_ChestESP13" local chestEnabled = false local chestAddedConn = nil local chestBoxRenderConn = nil local trackedChests = {} local function isChestPart(obj) if obj:FindFirstChild(CHEST_TAG) then return true end if obj.Parent and obj.Parent:FindFirstChild(CHEST_TAG) then return true end if obj.Parent and obj.Parent.Parent and obj.Parent.Parent:FindFirstChild(CHEST_TAG) then return true end return false end local function addChestHighlight(obj) if not obj or not obj.Parent then return end if obj:FindFirstChild(CHEST_TAG) then return end local hl = Instance.new("Highlight") hl.Name = CHEST_TAG hl.FillColor = Color3.fromRGB(255, 0, 0) hl.FillTransparency = 0.1 hl.OutlineColor = Color3.fromRGB(255, 0, 0) hl.OutlineTransparency = 0 hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop hl.Adornee = obj hl.Parent = obj end local function removeAllChestHighlights() for _, obj in pairs(workspace:GetDescendants()) do if obj:IsA("Highlight") and obj.Name == CHEST_TAG then obj:Destroy() end end end local function getRainbowColor() local hue = (tick() * 0.5) % 1 return Color3.fromHSV(hue, 1, 1) end local function createBoxFor(obj) if trackedChests[obj] then return end local lines = {} for i = 1, 12 do lines[i] = Drawing.new("Line") lines[i].Visible = false lines[i].Thickness = 2 lines[i].Transparency = 1 end trackedChests[obj] = lines end local function removeBoxFor(obj) local lines = trackedChests[obj] if not lines then return end for _, line in ipairs(lines) do line:Remove() end trackedChests[obj] = nil end local function getBoxCFrameAndSize(obj) if obj:IsA("BasePart") then return obj.CFrame, obj.Size elseif obj:IsA("Model") then local ok, cf, size = pcall(function() return obj:GetBoundingBox() end) if ok then return cf, size end end return nil, nil end local function startChestBoxRender() if chestBoxRenderConn then chestBoxRenderConn:Disconnect() end chestBoxRenderConn = RunService.RenderStepped:Connect(function() if not chestEnabled then return end local color = getRainbowColor() local cam = workspace.CurrentCamera for obj, lines in pairs(trackedChests) do if not obj or not obj.Parent then removeBoxFor(obj) else local cf, size = getBoxCFrameAndSize(obj) if cf and size then local half = size / 2 local corners = { cf * CFrame.new(-half.X, -half.Y, -half.Z), cf * CFrame.new( half.X, -half.Y, -half.Z), cf * CFrame.new( half.X, half.Y, -half.Z), cf * CFrame.new(-half.X, half.Y, -half.Z), cf * CFrame.new(-half.X, -half.Y, half.Z), cf * CFrame.new( half.X, -half.Y, half.Z), cf * CFrame.new( half.X, half.Y, half.Z), cf * CFrame.new(-half.X, half.Y, half.Z), } local positions = {} for i, corner in ipairs(corners) do positions[i] = corner.Position end local edges = { {1,2},{2,3},{3,4},{4,1}, {5,6},{6,7},{7,8},{8,5}, {1,5},{2,6},{3,7},{4,8}, } for i, edge in ipairs(edges) do local p1, p2 = positions[edge[1]], positions[edge[2]] local sp1, on1 = cam:WorldToViewportPoint(p1) local sp2, on2 = cam:WorldToViewportPoint(p2) if on1 and on2 then lines[i].From = Vector2.new(sp1.X, sp1.Y) lines[i].To = Vector2.new(sp2.X, sp2.Y) lines[i].Color = color lines[i].Visible = true else lines[i].Visible = false end end end end end end) end local function stopChestBoxRender() if chestBoxRenderConn then chestBoxRenderConn:Disconnect() chestBoxRenderConn = nil end for obj, _ in pairs(trackedChests) do removeBoxFor(obj) end trackedChests = {} end local function getScanRoots() local roots = {} for _, obj in ipairs(workspace:GetChildren()) do table.insert(roots, obj) end return roots end local function scanForChests() local queue = {} for _, obj in ipairs(getScanRoots()) do table.insert(queue, {obj = obj, depth = 0}) end local idx = 1 while idx <= #queue do local item = queue[idx] idx = idx + 1 local obj = item.obj local depth = item.depth if obj and obj.Parent then if isChestObj(obj) then addChestHighlight(obj) createBoxFor(obj) end if depth < 3 then for _, child in ipairs(obj:GetChildren()) do table.insert(queue, {obj = child, depth = depth + 1}) end end end end end local function startChestESP() scanForChests() startChestBoxRender() if chestAddedConn then chestAddedConn:Disconnect() end chestAddedConn = workspace.DescendantAdded:Connect(function(obj) if not chestEnabled then return end if isChestObj(obj) then addChestHighlight(obj) createBoxFor(obj) end end) end local function stopChestESP() if chestAddedConn then chestAddedConn:Disconnect() chestAddedConn = nil end stopChestBoxRender() removeAllChestHighlights() end local xrayAddedConn = nil local function applyXray(obj) if obj:IsA("BasePart") then if isChestPart(obj) then return end if not obj.Parent:FindFirstChildWhichIsA("Humanoid") and not (obj.Parent and obj.Parent.Parent and obj.Parent.Parent:FindFirstChildWhichIsA("Humanoid")) then obj.LocalTransparencyModifier = 0.5 end end end MiscTab:CreateToggle({ Name = "透视物体", CurrentValue = false, Callback = function(val) if val then _G.XrayOn = true for _, v in pairs(workspace:GetDescendants()) do applyXray(v) end if xrayAddedConn then xrayAddedConn:Disconnect() end xrayAddedConn = workspace.DescendantAdded:Connect(function(obj) if _G.XrayOn then applyXray(obj) end end) else _G.XrayOn = false if xrayAddedConn then xrayAddedConn:Disconnect() xrayAddedConn = nil end for _, v in pairs(workspace:GetDescendants()) do if v:IsA("BasePart") then v.LocalTransparencyModifier = 0 end end end end, }) MiscTab:CreateToggle({ Name = "高亮宝箱", CurrentValue = false, Callback = function(val) chestEnabled = val if val then startChestESP() else stopChestESP() end end, }) local espPlayerCons = {} local boxDrawings = {} local boxRenderConn = nil local espPlayerAddedConn = nil local persPlayerEnabled = false local function addESP(character) if not character then return end local oldHL = character:FindFirstChild("NH_ESP") if oldHL then oldHL:Destroy() end local highlight = Instance.new("Highlight") highlight.Name = "NH_ESP" highlight.FillTransparency = 0.7 highlight.OutlineTransparency = 0 highlight.FillColor = Color3.fromRGB(255, 0, 0) highlight.OutlineColor = Color3.fromRGB(255, 255, 255) highlight.Adornee = character highlight.Parent = character end local function hookPlayer(plr) if plr == LocalPlayer then return end if plr.Character then addESP(plr.Character) end local con = plr.CharacterAdded:Connect(function(char) task.wait(0.2) addESP(char) end) table.insert(espPlayerCons, con) end local function createBoxDrawings(plr) if boxDrawings[plr] then return end local lines = {} for i = 1, 12 do lines[i] = Drawing.new("Line") lines[i].Visible = false lines[i].Thickness = 2 lines[i].Transparency = 1 end boxDrawings[plr] = lines end local function removeBoxDrawings(plr) local lines = boxDrawings[plr] if not lines then return end for _, line in ipairs(lines) do line:Remove() end boxDrawings[plr] = nil end local function startPlayerBoxRender() if boxRenderConn then boxRenderConn:Disconnect() end boxRenderConn = RunService.RenderStepped:Connect(function() if not persPlayerEnabled then return end local color = getRainbowColor() for _, plr in ipairs(Players:GetPlayers()) do if plr ~= LocalPlayer then local character = plr.Character local humanoid = character and character:FindFirstChildOfClass("Humanoid") local root = character and character:FindFirstChild("HumanoidRootPart") if character and humanoid and humanoid.Health > 0 and root then createBoxDrawings(plr) local lines = boxDrawings[plr] local cf = root.CFrame local size = Vector3.new(4, 6, 2) local half = size / 2 local corners = { cf * CFrame.new(-half.X, -half.Y, -half.Z), cf * CFrame.new( half.X, -half.Y, -half.Z), cf * CFrame.new( half.X, half.Y, -half.Z), cf * CFrame.new(-half.X, half.Y, -half.Z), cf * CFrame.new(-half.X, -half.Y, half.Z), cf * CFrame.new( half.X, -half.Y, half.Z), cf * CFrame.new( half.X, half.Y, half.Z), cf * CFrame.new(-half.X, half.Y, half.Z), } local positions = {} for i, corner in ipairs(corners) do positions[i] = corner.Position end local edges = { {1,2},{2,3},{3,4},{4,1}, {5,6},{6,7},{7,8},{8,5}, {1,5},{2,6},{3,7},{4,8}, } for i, edge in ipairs(edges) do local p1, p2 = positions[edge[1]], positions[edge[2]] local sp1, on1 = workspace.CurrentCamera:WorldToViewportPoint(p1) local sp2, on2 = workspace.CurrentCamera:WorldToViewportPoint(p2) if on1 and on2 then lines[i].From = Vector2.new(sp1.X, sp1.Y) lines[i].To = Vector2.new(sp2.X, sp2.Y) lines[i].Color = color lines[i].Visible = true else lines[i].Visible = false end end else if boxDrawings[plr] then for _, line in ipairs(boxDrawings[plr]) do line.Visible = false end end end end end end) end local function stopPlayerBoxRender() if boxRenderConn then boxRenderConn:Disconnect() boxRenderConn = nil end for plr, lines in pairs(boxDrawings) do for _, line in ipairs(lines) do line:Remove() end end boxDrawings = {} end MiscTab:CreateToggle({ Name = "透视玩家", CurrentValue = false, Callback = function(val) persPlayerEnabled = val if val then for _, plr in ipairs(Players:GetPlayers()) do hookPlayer(plr) end if espPlayerAddedConn then espPlayerAddedConn:Disconnect() end espPlayerAddedConn = Players.PlayerAdded:Connect(function(plr) if persPlayerEnabled then hookPlayer(plr) end end) startPlayerBoxRender() else if espPlayerAddedConn then espPlayerAddedConn:Disconnect() espPlayerAddedConn = nil end for _, con in ipairs(espPlayerCons) do con:Disconnect() end espPlayerCons = {} for _, plr in ipairs(Players:GetPlayers()) do if plr.Character then for _, desc in pairs(plr.Character:GetDescendants()) do if desc:IsA("Highlight") and desc.Name == "NH_ESP" then desc:Destroy() end end end end for _, hl in pairs(workspace:GetDescendants()) do if hl:IsA("Highlight") and hl.Name == "NH_ESP" then hl:Destroy() end end stopPlayerBoxRender() end end, }) local infJumpConn = UserInputService.JumpRequest:Connect(function() if LocalPlayer and LocalPlayer.Character then local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid") if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end end end) _G.InfJumpCon = infJumpConn local antiAFKConn = LocalPlayer.Idled:Connect(function() VirtualUser:CaptureController() VirtualUser:ClickButton2(Vector2.new()) end) _G.AntiAFKCon = antiAFKConn local noclipConn = RunService.Stepped:Connect(function() local char = LocalPlayer.Character if not char then return end for _, part in pairs(char:GetDescendants()) do if part:IsA("BasePart") then part.CanCollide = false end end end) Rayfield:Notify({ Title = "加载完毕", Content = "浪费时间脚本已加载", Duration = 4, })
